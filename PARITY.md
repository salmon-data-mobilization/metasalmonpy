# Parity deviations — metasalmonpy vs metasalmon

The mirror contract (see `AGENTS.md`) requires the same behaviour and
capabilities as metasalmon at the version this package claims — **not**
literal API mimicry. This register lists every deliberate difference; its
twin lives in the hub at `metasalmon/knowledge/parity-deviations.md`, and the
two must agree. An undocumented difference is a contract violation even when
the difference itself is fine (Brett, 2026-08-15).

Kinds of entries:

- **Idiom** — same behaviour, Pythonic delivery.
- **Ahead** — Python already has semantics R adopted later (or improved on);
  R converges or stays. Marked *converged* once metasalmon ships them; the row
  stays and keeps its number, because this package still claims an earlier
  parity milestone and the row is what tells a reader the difference is era
  lag rather than a standing choice.
- **Inapplicable** — the R behaviour has no Python counterpart, with the why.
- **Gap** — a capability metasalmon has and this package does not yet. Not an
  idiom and not a licence to stay behind: every Gap row names the milestone
  that closes it.

Row numbers are permanent in both registers so the two can be cross-referenced
by number.

| # | Kind | Difference | Why |
|---|---|---|---|
| 1 | Idiom | Errors are Python exceptions with actionable messages, not R cli conditions | Language idiom; same conditions trigger them |
| 2 | Idiom | Optional dependencies are extras (`metasalmonpy[eml]`, `[knb]`), not Suggests | Packaging idiom; same lazy-guard behaviour and install pointers |
| 3 | Idiom | Canonical ordering uses codepoint `sorted()`, not explicit C-collation flags | Python's default sort is already locale-independent; the *contract* (locale-independent deterministic output) is identical, and `locale.strxfrm` is banned |
| 4 | Idiom | Archive/EML/ORE parity is contract-level (structure, manifest, ordering, fail-closed), never byte-level | R's bytes come from `zip`/libxml2 formatters Python cannot and should not reproduce |
| 5 | Ahead | `infer_value_type` is public API here, internal in R | Was already exported at 0.1.6; removing it would break users for no capability gain |
| 6 | Ahead | A failed or empty ontology fetch raises; 0.1.6-era R returned an empty index | Failed lookup ≠ empty lookup; R adopted the same principle at 0.2.2 |
| 7 | Ahead | `is_statistical_modifier` is a real column in both term-index frames; R's TTL path carries it only inside `role_hints` | Saves the 0.3.0 milestone a retrofit; hint strings match R exactly |
| 8 | Inapplicable | No interactive term-request console, so R 0.2.0's cancel-must-not-submit semantics have no counterpart | Submission here is a single explicit function call with confirmation |
| 9 | Inapplicable (landed 0.1.8) | `write_sdp_methods` is **not implemented**: `metadata/methods.csv` is read and validated only. The name is exported and raises `NotImplementedError` with the reason rather than being absent | No Python user ever existed at 0.1.8 parity needing to *author* a registry, while Python does receive R-written packages carrying one. SDP 0.3.0 removes `metadata/methods.csv` from the specification entirely and replaces it with `migrate_sdp_methods()`, so a writer would be built only to be deleted in the same replay, and every package it produced would need migrating. The reader and validator survive that transition — a migration needs them. Logged in the S10 execplan (2026-08-15). A raising stub rather than a missing attribute so a user who looks for the R function gets the reason, not an `AttributeError`. **Retirement condition:** delete the stub (and `tests/test_sdp_methods.py::test_the_writer_is_absent_and_says_why`) at the 0.3.0 rung, when the registry stops existing |
| 10 | Idiom | The SSSOM embedded metadata header is parsed with a restricted YAML-subset parser (plain/quoted scalars, one-level block mappings, block sequences); R parses it with the `yaml` package | Dependency policy is pandas+requests only; the subset is exactly what the canonical writer emits and the SDP profile uses. Headers outside the subset raise the same "not valid YAML" report R gives malformed YAML, and duplicate-key/quoting/comment behaviours are differentially tested against R. Canonical mapping-set bytes match R's under C collation (see row 20) |
| 11 | Idiom | `mapping-sets.json` provenance records `metasalmonpy.write_sdp_sssom` + `metasalmonpy_version`; `validate_sdp_sssom` accepts either implementation's provenance (metasalmon accepts metasalmonpy provenance since PR #43; each validator now accepts either) | Honest provenance: a manifest must name the writer that produced it. Dual acceptance keeps R-written SDPs valid here. Everything else in the manifest — entry fields, sha256 binding, radix `mapping_set_id` ordering — matches R exactly; the mapping-set TSV artifacts themselves are byte-identical |
| 12 | Idiom | `measurement-decompositions.json` provenance records `metasalmonpy.write_sdp_measurement_decompositions` + `metasalmonpy_version`; the validator accepts either implementation's provenance | The same honest-provenance ruling as row 11, applied to the 0.1.7 decomposition artifact. The artifact binding (path, sha256, row_count) and the decomposition CSV bytes themselves are byte-identical to R's (asserted against R-generated fixtures in `tests/data/decompositions/`) |
| 13 | Ahead | A dangling symlink at a managed output path is treated as existing (blocked without `overwrite`, then refused as a symlink) in `write_sdp_sssom` and `write_sdp_measurement_decompositions`; R's `file.exists()` misses dangling symlinks and silently writes through/over them | Fail-closed writer safety: a symlink the writer did not create should never be replaced implicitly. Read-side symlink handling matches R exactly (only resolvable symlinks are refused; dangling ones surface as missing files) |
| 14 | Idiom | The EML mapping sidecar is parsed with PyYAML (in the `[eml]` extra), not the restricted YAML-subset parser used for SSSOM headers | The sidecar is genuinely nested (creators, contacts, per-table attribute configs), unlike the flat SSSOM header; R uses `yaml::read_yaml` here too. Core dependencies stay pandas+requests |
| 15 | Idiom | EML documents are built with stdlib ElementTree and validated with lxml; parity with R is structural (`ET.canonicalize`), not byte-level | R serializes through libxml2, so whitespace differs while the documents are identical. lxml IS libxml2 — the engine behind `emld::eml_validate` — so accept/reject matches R by construction, and all UUIDv5 identifiers match exactly |
| 16 | Idiom | The KNB adapter speaks the DataONE v2 REST API directly with `requests`; R's default adapter delegates to `dataone`/`datapack`. The adapter boundary is a class with exactly the fourteen v0.1.7 methods, injected through `set_knb_adapter()` instead of R's `metasalmon.knb_adapter` option, and the JWT is supplied through `set_dataone_token()` instead of R's `dataone_token` option | There is no maintained Python `dataone`/`datapack`, and R already bypasses them for the ORE, the anonymous probes, the format registry, and the Solr catalog. Consequences of speaking REST directly, all behaviour-preserving: the production CN base URL is an explicit constant (R resolves it through `D1Client`); `update_object` uses the spec resource path `PUT /object/{oldPid}` where R's `dataone` client PUTs to `/object` with `pid` as a form field; `authoritativeMemberNode` is filled in `_system_metadata_document` (whose `node_id` argument R accepts and leaves unused) because R lets `dataone::createObject()` attach it at the upload boundary — the uploaded documents are `ET.canonicalize`-equal to R's; and the JWT claim decode is stdlib base64+json, matching R's unverified local check (the real check is server-side `echoCredentials`), so no PyJWT |
| 17 | Idiom | The deterministic SDP ZIP has its own Python determinism reference (`metasalmonpy-zipfile-1`: radix member order, 2000-01-01 timestamps, 0644, Unix `create_system`, no directory entries, deflate level 9) instead of R's reviewed-`zip`-version guard, and its bytes are not comparable to R's | Row 4 applied to the archive. miniz (R's `zip`) and zlib emit different deflate streams, so byte parity is impossible without vendoring a compressor. **R's guard is an allowlist, not a single-version pin** (corrected 2026-08-16): `.ms_knb_reviewed_zip_versions <- c("3.0.1", "3.0.2")` in `R/knb-sdp-archive.R`, with a runtime abort, so a new compressor version is byte-compared against a reviewed one and then added. The guard's *purpose* — a compressor change must be a reviewed package change, because the archive checksum is an immutable DataONE PID input — is kept here by asserting the reference sha256 in `tests/data/knb/expected.json`; there is no runtime version abort because zlib ships with the interpreter and cannot be pinned per platform |
| 18 | Idiom | Because the ZIP (row 17), the EML document (row 15) and the ORE document (row 4) differ from R's *bytes*, an end-to-end Python plan for the same SDP yields a different archive PID, resource-map PID and `plan_sha256` than R's | Unavoidable and disclosed, not silent: every algorithm that derives those values is pinned against R by driving the planner from R's own member table (`tests/data/knb/r/`). **Two different strengths of assertion, stated exactly** (corrected 2026-08-16): the recovery-manifest JSON, the fingerprint payload encoding and its SHA-256, and the resource-map PID preimage are compared as **literal bytes**; the ORE graph and the nine SystemMetadata documents are compared with `ET.canonicalize(..., strip_text=True)` and are **canonicalization-equal, not byte-equal** — the earlier "byte-exact … the ORE graph" wording overstated that half, and row 16's phrasing is the honest one. A manifest written by one implementation is therefore reviewed and published by that same implementation, exactly as DataONE immutability already requires |
| 19 | Idiom | `publish_sdp_to_knb(confirm=...)` defaults to `None`, not R's `interactive()` + `missing(confirm)` pair | R needs both halves to guarantee that an interactive default can never authorize a live call; a `None` sentinel expresses the same rule in one place. Verified equivalent, not merely analogous: both sides demand exactly `TRUE`/`True` (R's `isTRUE()`, Python's `is True`), so a truthy `1` or `"yes"` authorizes nothing on either side |
| 20 | Ahead (converged at R 0.2.0) | Canonical SSSOM ordering (curie-map prefixes, mapping-set ids) is codepoint/C here. Era R (0.1.7/0.1.8) used bare `sort()`/`order()`, so its bytes and manifest order vary by `LC_COLLATE` | **metasalmon adopted C collation at 0.2.0, not 0.3.0** (corrected 2026-08-16 — NEWS 0.2.0, "Canonical bytes and identifiers no longer depend on `LC_COLLATE`", commit `2f2074e`; `R/sssom.R` orders with `method = "radix"` at every site reaching bytes or the manifest). Current R and Python agree. **Interop hazard, live only against era R (≤ 0.1.8):** an era-R package written in a non-C locale can fail this validator's manifest-ordering check, and vice versa (demonstrated both directions). Committed `tests/data/sssom/` fixtures were generated under `LC_COLLATE=C`. Practical consequence for the replay: a byte-equality claim measured against era R needs its locale caveat; one measured against R 0.2.0+ does not |
| 21 | Ahead (converged at R 0.2.4) | Every SDP reader preserves a literal `"NA"`; only the empty field is missing. Era R used readr's `na = c("", "NA")` | A forward-port of metasalmon **0.2.4**'s canonical missing-value contract ("NA" is a real fisheries gear code), landed early because era behaviour combined with the already-forward-ported `package_io` reader made a literal-`"NA"` vocabulary row unsatisfiable: Python could not export a package R could read. Residual difference is representation, not the token set — an empty field parses to `NA_character_` in R and to `""` here |
| 22 | Interim (0.1.7-era only) — **live divergence** | Consequence of row 21: `_ERA_NA_TOKENS` still governs the EML raw-token audit (era `("", "NA")`), so that audit models era R's parse of a **data resource** — a trimmed `NA` is parsed-missing there — while this package's own readers keep it as data | An internal split inherited from applying row 21 at the 0.1.7 milestone. **It changes verdicts against current R** (corrected 2026-08-16; the earlier "cannot make an audit verdict disagree with R's" was true of era R and is false against 0.3.0): R's `.ms_eml_add_missing_values()` derives missingness from `is.na(parsed_values)` and R's sole NA token is now `""`, so a literal `NA` cell with no declared missing-value code **is accepted by R and rejected here** as an undeclared non-empty missing token. Treat this as an active interop hazard for any package exchanged with metasalmon ≥ 0.2.4, not a dormant note. **Retirement condition:** delete `_ERA_NA_TOKENS` and read the audit through the same empty-field-only token as every other reader when the replay reaches the 0.2.4 rung; the divergence closes with it |
| 23 | Idiom | `trim_ws` is an approximation of readr, not a transliteration: `skipinitialspace=True` plus a strip of readr's trim characters (`" \t\r\n"`) | Narrow enough to document rather than chase; recorded so it is a known boundary and not a surprise. **The boundary is now measured and pinned** (2026-08-16): readr trims a leading tab *before* looking for an opening quote, pandas' `skipinitialspace` skips only spaces, so `\t"z"\t` reads as `z` in R and `"z"` here. Confirmed against R 4.5.2 and pinned by `tests/test_sdp_csv_reader.py::test_a_tab_before_an_opening_quote_is_the_one_known_trim_mismatch`; every other probed shape in `READR_ROW` agrees, and the shape appears in no canonical SDP artifact |
| 24 | Idiom | Atomic writes go through a Python-only `atomic_io` module so artifacts get the umask default mode | R gets umask behaviour free from `writeBin`; Python's `tempfile.mkstemp()` hard-codes 0600, which would have shipped owner-only semantic artifacts. The module **measures** the default (one throwaway `os.open(..., 0o666)` then `fstat`, so the kernel has already subtracted the umask) rather than mutating and restoring the process umask, which is a per-process race; `tests/test_atomic_io.py::test_default_file_mode_never_calls_os_umask` guards that. **Every published artifact must route through it.** KNB was the one bypass and was fixed at 0.1.8: `_atomic_write_raw` inlined `mkstemp` + `os.replace`, publishing the recovery manifest, resource map and SDP archive at 0600 |
| 25 | Gap (0.2.0 rung) | `read_salmon_datapackage()` returns every resource column as a string, preserving the raw token; era R type-inferred resources with `readr::read_csv(file_path)` | **The text read converged at R 0.2.0** — `.ms_read_resource_csv()` reads all-character and the dictionary became the sole type authority — but **the typing did not**, so this is a capability gap rather than an idiom (reclassified 2026-08-16). R 0.3.0 converts each dictionary-declared column in memory, keeps the raw token as ground truth, and raises a structured validation issue when a conversion is not faithful; here a caller gets `str` and coerces themselves. Second consequence of routing every SDP CSV through `read_sdp_csv` (row 21): pandas' own inference could not be kept once a literal `"NA"` was preserved, because a mixed column carrying one is `object` anyway and pandas' inference is not readr's. **Closes at the 0.2.0 rung**, which owns the typed round-trip reader. Verified consequence on both sides: a whitespace-padded resource header is trimmed, so `_validate_raw_table` aborts on it exactly as `.ms_eml_validate_raw_table()` does |
| 26 | Idiom | `_is_canonical_rights_url()` expresses R's `url_parse()`/`url_build()` round-trip test as explicit conditions instead of reimplementing curl's URL normalizer | R accepts a custom rights URL only when it is already in curl's canonical form. Python has no curl URL parser and vendoring one for a licence check is disproportionate, so the same rule is written as the things curl would otherwise change: lowercase scheme, non-empty host, non-empty path, no whitespace, no dot segments, and a `urlsplit`/`urlunsplit` round trip. Verdicts match era R on **all 21** pinned probe values — 13 accepted, 8 rejected (count corrected 2026-08-16; the register previously said twenty) — in `tests/test_package_io.py::test_licence_descriptors_match_era_r`. **Percent-encoding is a measured exception, not an agreeing case:** curl decodes an encoded separator and uppercases hex digits, so R rejects `%2F` and lowercase `%c3%a9` while these conditions accept both (`%20` and uppercase `%C3%A9` agree). Pinned by `test_percent_encoded_rights_urls_are_a_documented_divergence`; no SDP licence field has carried a percent-encoded path |
| 27 | Idiom (era lag) | The SDP profile version is read from the **vendored `sdp-0.2.0` bundle** (`sdp_schema.sdp_profile_version()`, re-exported as `metadata.SDP_PROFILE_VERSION`); R resolves it through `.ms_sdp_profile_version()` | Same single-source-of-truth property, and the 0.1.7 constant's retirement condition — "replace it with a read of the vendored rules bundle when 0.1.8 lands that bundle" — is now discharged. **The values differ** (corrected 2026-08-16; the earlier "same observable value" no longer holds): metasalmon 0.3.0 declares `sdp-0.3.0`, this package emits `sdp-0.2.0` into `dataset.csv$spec_version` and `datapackage.json`'s `sdp.specVersion`. Correct for a 0.1.8 parity claim and era lag rather than a standing choice, but the two implementations now stamp different spec versions. **The R mechanism also moved:** `.ms_sdp_profile_version()` returns `.ms_load_sdp_schema()$version`, remote-first against the pinned spec tag with the vendored bundle as fallback; this package has no remote loader until the 0.2.0 rung, which owns it. **Retirement condition:** the version stops being era-lagged when the replay reaches 0.3.0 and swaps the vendored bundle |
| 28 | Idiom | R's TRE-resolved `[[:space:]]`/`[[:cntrl:]]` memberships live once in `metadata` (`R_SPACE_CLASS`, `R_CNTRL_CLASS`) and are imported by `eml` and `sssom`; R re-writes the POSIX class at each call site | The enumeration is the load-bearing part and duplicating it is how `sssom` drifted from `eml` in the first place. **Retirement condition:** the constants stay while metasalmon resolves those classes through TRE; if metasalmon ever switches those validators to `perl = TRUE`, the replacement membership must be re-enumerated against that release, not guessed — PCRE's `[[:space:]]` is ASCII-only, which is why `measurement_decompositions` correctly keeps an ASCII class |
| 29 | Idiom | `reproducibility/manifest.json` provenance records `metasalmonpy.write_sdp_reproducibility_manifest` + `metasalmonpy_version`; the validator accepts either implementation's provenance | The row-11 honest-provenance ruling applied to the 0.1.8 reproducibility manifest. Verified 2026-08-16: for the same declared artifact set the manifest is **byte-identical to R's apart from those two provenance values** — profile string, artifact ordering (radix `path`), `sha256`, `size_bytes`, and the JSON encoding all match, and this reader validates an R-written manifest unchanged |
| 30 | Idiom | `sdp_schema.sdp_profile_version()` reads the single top-level `version:` line out of the vendored `sdp.rules.yaml` with a line scan; R parses the whole document with `yaml::read_yaml` | Core dependencies are pandas + requests, and pulling PyYAML into the core for one scalar is disproportionate (PyYAML stays in the `[eml]` extra, row 14). The rules document is machine-generated by `smn-data-pkg`'s `scripts/generate_artifacts.py` and has carried `version:` as a plain top-level scalar in every published version; the scan **raises rather than guessing** if the key is absent, so a format change surfaces as an error and not a silently stale version. **How the core-deps property is enforced, not merely asserted:** this row claimed a property with nothing checking it, and the 0.1.8 milestone broke it in the same PR that wrote the row — see row 34. It is now enforced from two directions: the core-deps CI job runs the whole suite with neither extra installed, and `tests/test_knb_publication.py::KnbCoreDependencyTests` blocks `yaml` from `sys.meta_path` so the property also fails in a *developer's* environment, where the extra is normally present. **Retirement condition:** replace the scan with a full parse the day this package needs any *other* field of `sdp.rules.yaml` — at that point PyYAML is already justified and the narrow reader is a liability |
| 31 | Idiom | `apply_semantic_suggestions(strategy="llm")` requires the suggestion frame to carry `llm_selected`, `llm_decision` **and** `llm_confidence`, and applies only rows with `llm_selected` truthy *and* `llm_decision == "accept"`. R requires only `llm_selected` and filters on it alone | Stricter, and equivalent on any frame the pipeline produces: `suggest_semantics(llm_assess=TRUE)` sets `llm_selected` only for accepted candidates, so the extra condition is redundant there. It differs only for a hand-built suggestions frame that sets `llm_selected` without a decision — which this package refuses rather than silently applying an unreviewed IRI. `strategy="reviewed"` (added 0.1.8) matches R exactly: `decision` in `{accepted, accept}`, case-insensitive and trimmed, and multiple accepted constraints for one measurement are deduplicated in first-occurrence order and joined with `"; "` — verified identical to R's output for all three strategies |
| 32 | Gap (pre-0.1.6 debt, no milestone yet) | `_score_and_rank_terms` implements R's `match_type` ladder and its default weights, but not R's wider **ranking-profile system**: there is no `ranking_profile` argument, no `.merge_ranking_profile`, and the base source weights and role boosts are hardcoded to values that differ from `.ranking_profile_defaults()` (e.g. `smn` 1.2 here vs 1.0 in R, `gcdfo` 1.0 vs 0.9) | Found and recorded while landing the 0.1.8 `match_type` fix, which is real here — a candidate whose optional `match_type` is missing now scores as unclassified (`other`, 0.05) instead of being ignored, matching `.match_type_score_profiled()` on every probed value. The surrounding divergence predates the 0.1.6 claim and was never registered; it changes candidate *ordering* for the same query, so it is a capability gap rather than an idiom. It is **not** in the 0.1.8 rung's scope and no milestone owns it yet — recorded so it stops being invisible, and flagged to the hub for sequencing |
| 33 | Gap (open) | `_is_absolute_iri()` (`sdp_methods.py:95`) matches whitespace with Python's `\s` (`_ABSOLUTE_IRI_RE`, `sdp_methods.py:67`; `_HTTP_AUTHORITY_RE`, `:69`) instead of importing `metadata.R_SPACE_CLASS` — even though its own docstring names `.ms_sdp_extension_is_absolute_iri` as the thing it mirrors. `\s` on a `str` pattern is Unicode-aware but is **not** TRE's `[[:space:]]` | Not a deliberate deviation: this is row 28's drift a second time, in the one extension module that never imported the constants. `eml.py:92` and `sssom.py:234` both build their patterns from `R_SPACE_CLASS`; `sdp_methods.py` never did, and `tests/test_sdp_methods.py` carries no whitespace-membership test where `tests/test_eml.py` and `tests/test_sssom.py` both do — which is why nothing caught it. **Measured, not guessed**, on R 4.5.2 across U+0001–U+3100: against metasalmon post-fix the two engines disagree on exactly **8 codepoints — U+001C, U+001D, U+001E, U+001F, U+0085, U+00A0, U+2007, U+202F — and this package rejects every one of them where R accepts.** This package is therefore the stricter side, so the failure mode is an SDP-extension IRI written here being refused by this validator and accepted by metasalmon, not the reverse; it reaches users through `validate_sdp_methods()` (`sdp_methods.py:575`, `:578`), `observation_structures.py:256`, and KNB publication, which calls both. **The gap narrowed rather than widened when metasalmon fixed its own side:** against R *before* that fix the disagreement was 23 codepoints, because `.ms_sdp_extension_is_absolute_iri()` ran under PCRE, whose `[[:space:]]` is only the six ASCII characters — aligning R on TRE cut it to 8. Found from the hub side while landing that fix (metasalmon backlog #85/#86), by testing row 28's claim that "Python mirrors no such function"; it does, here. **Retirement condition:** `sdp_methods.py` imports `R_SPACE_CLASS` from `metadata` and builds both regexes from it exactly as `eml.py` and `sssom.py` already do, plus a whitespace-membership test in `tests/test_sdp_methods.py` matching the ones in `tests/test_eml.py` and `tests/test_sssom.py`. Then this row is **deleted rather than converged** — there is nothing here worth keeping. Hub twin: `metasalmon/knowledge/parity-deviations.md` row 33 |
| 34 | Idiom | The reviewed-ledger **binding** assertions live in the publication preflight (`_require_review_ledger_binding`, called from `_build_plan`), not inside the artifact-inventory helper. R keeps both the inventory and the assertions inside `.ms_knb_sdp_artifact_paths()` | **R has no core/optional dependency split to respect** — `yaml` is a hard Import for metasalmon — so its helper layout encodes a constraint R does not actually have. Python does have one: PyYAML is in the `[eml]` extra. Mirroring the layout literally imported that non-constraint and broke a real property, making the deterministic SDP archive (pure pandas + `zipfile`) require the extra to build and removing eight archive tests from core-deps coverage. Parity is behavioural, not literal (Brett, 2026-08-15), and behaviour is unchanged for every reachable caller: nothing about the inventory ever needed the mapping — which ledger is published is decided by what is on disk — and `publish_sdp_to_knb()` has required the extra long before the assertions run. They now fire ahead of both representation branches and before any archive is written, which is **earlier** than R fires them. A package with neither ledger is deliberately left to the inventory helper so it still reports R's "requires a reviewed semantic-selection ledger" message rather than a binding complaint about a file that does not exist. **Do not move these back into the inventory helper** — that is the regression, and `KnbCoreDependencyTests` exists to fail when someone does |

Maintenance: add a row in the same PR that introduces the difference, and
mirror it to the hub register in the same stream. Rows 16–28 were re-verified
against metasalmon 0.3.0 on 2026-08-16 by reading both implementations; rows
17, 18, 20, 22, 23, 25, 26 and 27 carry the corrections that verification
produced.
