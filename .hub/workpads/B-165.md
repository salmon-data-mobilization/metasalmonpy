# B-165 — Port `write_sdp_semantic_closure()` to metasalmonpy, the mirror half of B-116

Hub queue **B-165** (P1, stream S10, repo `metasalmonpy`, blocked by B-116 which
is `done`). The R half is metasalmon pull request **#121**; the specification is
*What metasalmon 0.5.0 owes the mirror* in metasalmon's
`knowledge/parity-deviations.md`, deliberately not restated in the item.

Owed as a **port**, not as design: absence in Python was lag. So **no new
`PARITY.md` row**, and nothing here changes what the mirror contract claims —
the `0.4.0 → 0.5.0` window stays open and the version stays `0.4.0`, because
this is a *post-0.5.0 development-version* behaviour, the same class as B-124,
B-125 and B-144.

## What changed, and where

| File | Change |
|---|---|
| `semantic_closure.py` | **New.** `write_sdp_semantic_closure(path, evidence=None, search_fn=find_terms, sources=("smn","gcdfo"), quiet=False)` and its helpers. Mirrors `R/semantic-closure.R`. |
| `term_search.py` | `_DEGRADED_STATUSES` and `_search_failed_sources()` hoisted out of `find_terms()`; `find_terms()` now reads them for its own warning. |
| `term_requests.py` | `namespace_scope` hoisted out of `render_ontology_term_request()` to module-level `_namespace_scope()`. |
| `__init__.py` | Exports `write_sdp_semantic_closure`. |
| `guides/semantic-review.qmd` | New *Assemble the reviewed closure* section. |
| `CHANGELOG.md` | Entry under Unreleased → Added. |
| `tests/test_semantic_closure.py` | **New**, 29 tests, mirroring `tests/testthat/test-semantic-closure.R` test for test. |

The producer reuses the package's own layers rather than adding parallel ones:
`eml._canonical_measurement_iris` / `_canonical_review_targets` /
`_vocabulary_snapshot_sha256`, `sdp_methods._extension_root` /
`_assert_safe_directory` / `_assert_safe_file` / `_atomic_write_set` /
`_csv_bytes`, `term_search.find_terms`, `term_requests.GAP_COLUMNS`,
`text_safety.redact_secrets`.

## The five #121 review findings, each carried across

1. **A degraded lookup aborts; it does not become a gap.** A raised search error
   and the `attrs["diagnostics"]` failed-source signal are both carried back out
   of `_search_evidence()`. An IRI that failed to resolve while any attempted
   search was degraded raises `RuntimeError` **before anything is written**,
   naming the IRI and the sources that did not answer.
2. **Incomplete evidence is a third outcome, not a gap.** `closure["incomplete"]`
   names the IRI, the missing field and the slot, and warns in as many words that
   it is not an ontology gap. Three ways a row can go unwritten; only one makes a
   gap.
3. **All three outputs install as one link-refusing atomic set**, through
   `sdp_methods._atomic_write_set()`, with both sidecar digests computed over the
   bytes *about to be installed* (`_bytes_sha256`) rather than by hashing files
   already on disk — which is what lets the sidecar join the same write set.
4. **Path resolution goes through the package's own hardened layer.** Symlinked
   root, symlinked intermediate component and symlinked final entry are all
   refused, for both closure targets **and the sidecar itself** (a linked sidecar
   would otherwise choose where the closure gets written). **Hard links are
   closed structurally by the staged rename rather than detected**, and
   `_resolve_write_path`'s docstring says so explicitly rather than implying
   coverage.
5. **The degraded-status test is one copy.** `find_terms()`'s warning and this
   producer both read `term_search._search_failed_sources()`.

The ruled **gap-not-abort** shape (Brett, 2026-09-12) is implemented as ruled and
is not softened into an exception on the Python side.

## Verification

metasalmonpy's own runner, both dependency legs CI runs (`.github/workflows/parity.yml`),
from a worktree-installed virtualenv each. Baselines are the tree at
`origin/main` `d471dd0`.

| Leg | Before | After |
|---|---|---|
| `[test,eml,context]`, R present | 907 passed / 1 skipped | **936 passed / 1 skipped** |
| `[test]` core only, R present | 794 passed / 114 skipped | **799 passed / 138 skipped** |

The core leg's five forbidden modules were asserted unimportable with CI's own
check step before the run. `tests/smoke.py`, `python -m build`, `uv lock --check`
and `git diff --check` all green on both legs. The R↔Python round trip
(`tests/test_roundtrip.py`, the `parity` job) passes with metasalmon installed.

### The evidence that matters: R and Python write the same bytes

Both producers driven over the same package (`tests/data/eml/sdp-default`, whose
shape matches R's `make_eml_test_sdp()` fixture: four vocabulary rows, five
review targets, differing by `smn:Observation`), with the same injected search
and the same `evidence`. metasalmon installed from `origin/main` `846bbab`
(post-#121), R 4.3.3.

| Scenario | `semantic_vocabulary.csv` | `reviewed_semantic_selections.csv` | `eml-mapping.yml` |
|---|---|---|---|
| Ordinary (4 vocab / 5 ledger rows) | identical | identical | identical |
| Gap (invented `term_iri`) | identical | identical | identical |
| Incomplete (found, blank definition) | identical | identical | identical |
| Empty (nothing annotated) | identical | identical | identical |
| Evidence overlay + scoped row | identical | identical | identical |

Ordinary-case digests, both sides:
`980c35e0…` vocabulary, `ad2cf185…` ledger, `7c86c0f2…` sidecar.

Field-level agreement where bytes are not the output:

- **Gap row**, every field equal including the prose: `no_candidates`,
  `term_iri`, `variable`, `candidate_count 0`, `placement_recommendation smn`,
  `target_sdp_file column_dictionary.csv`, `target_row_key count`,
  `search_query "no such term here"`, identical `placement_rationale`.
- **Incomplete row**: 1 row, `definition` missing, `resolved_source smn`,
  `term_iri`, `variable`, `searched_role variable`, 0 gaps, 3 vocabulary rows.
- **Degraded**: both abort over the same 4 IRIs naming `gcdfo`, and both leave
  neither file on disk.
- **Overlay**: supplied `label` wins, unsupplied `definition` still comes from
  the search, `native_type`/`source_url` derived from the resolved source; a
  `target_sdp_field`-scoped row wins for its slot and the IRI-wide row serves the
  rest.
- **Duplicate evidence rows** for one IRI and slot are refused by both, in the
  same helper.

### Failing-before, passing-after

This is a port rather than a defect fix, so the failing-before evidence is that
each ruled behaviour has a demonstrated RED: the behaviour was broken
deliberately and the test caught it.

| Behaviour broken | Test that failed |
|---|---|
| Swallow the raised search error (`except: pass`) | `…a_search_that_raises_aborts_rather_than_manufacturing_a_gap`, `…a_raised_search_error_is_redacted_where_it_is_captured` |
| Never read `attrs["diagnostics"]` | `…an_empty_result_with_failed_source_diagnostics_is_not_a_gap` |
| Route a found-but-blank term through the gap table | `…a_term_found_with_a_blank_required_field_is_incomplete_not_a_gap` |
| Install the three files one at a time | `…a_failure_in_the_third_write_leaves_the_first_two_unchanged` |
| Skip the hardened path resolution | `…a_linked_closure_output_is_refused_and_its_target_is_left_alone` |
| Narrow `_DEGRADED_STATUSES` to `("error",)` | `…an_empty_result_with_failed_source_diagnostics_is_not_a_gap`, `…the_degraded_status_test_is_the_one_find_terms_applies` |

**One of those REDs found a real defect in my own test**, and it is the reason
the atomicity test now ends the way it does. The first draft supplied the
"changed" definition on a `target_sdp_field`-scoped evidence row, which the
vocabulary overlay correctly ignores — so both halves compared identical bytes
against identical bytes and the test passed against a deliberately non-atomic
writer. The fix is the scoped row replaced by a general one **plus a final
assertion that the same call, once unblocked, really does write different
bytes**, so "unchanged" can never again be true for the wrong reason.

## Contracts this had to survive

- **Mirror contract.** A port: no new `PARITY.md` row, no change to the version
  or to what the mirror contract claims.
- **C collation / one value one rendering.** Every cell is coerced to text once,
  by `_closure_text()`, when the row is built; that same string is the sort key,
  the digest input and the CSV byte. Ordering is Python's `sorted()`, which is
  codepoint order. Pinned by
  `…both_files_are_written_in_c_collation_order`, which asserts the written file
  carries the order that was sorted, and by the byte-identity runs above.
- **cli-safety.** Python's half is redaction only (`PARITY.md` row 37 — the
  escape half is deliberately absent because an exception message is a finished
  string). The one piece of external text this producer captures is a search
  exception, redacted with `redact_secrets()` at capture, and
  `…a_raised_search_error_is_redacted_where_it_is_captured` pins it.
- **No LLM on this path.** Pinned by a raising binding on
  `llm_review.request_json` / `_request_json_with_retries`.
- **Every guard says what retires it.** See the next section.

## Guards, suppressions and their retirement conditions

| Guard | Retires when |
|---|---|
| `_require_yaml()` — PyYAML gate, fired only when the sidecar exists | PyYAML stops being optional, or the sidecar stops being YAML. Stated in the docstring together with **why there is no fallback**: the declared paths are the only thing stopping a write to a file the sidecar does not point at. |
| `_REQUIRES_YAML` skip on 24 of the 29 tests | Same condition. The five that run on both legs are deliberate: the two extra-independent tests exist precisely so the core leg still proves the no-sidecar path works and the gate names the extra. |
| `_source_url()` — restates the two index fetch URLs | `find_terms()` returns the artifact URL it resolved. |
| `_resolve_write_path()` — reimplements a create-if-absent safe-file check | `sdp_methods._assert_safe_file` grows a create-if-absent mode. |
| `_set_mapping_digest()` — line-wise YAML edit | The sidecar stops being a hand-edited file. |
| `_PLACEHOLDER_RATIONALE` — the `REVIEW REQUIRED:` marker | `accept_suggestion()` requires a `decision_reason` and `apply_sdp_semantics()` carries it through for every accepted row; then the branch is unreachable and the marker, the `placeholders` return value and the warning go together. |
| `_DEGRADED_STATUSES` / `_search_failed_sources()` | A failed lookup stops being representable as an empty result. |
| `_namespace_scope()` | Namespace resolution moves into the ontology registry it hard-codes. |

## Two Python-native choices, neither a deviation

Both are language idiom, not behaviour, so no register row is opened. Recorded
here and in the module docstring so a reader does not have to infer them.

1. **Exception classes.** Input problems raise `ValueError` (which
   `SdpExtensionError` already subclasses, so path refusals stay catchable both
   ways); a degraded lookup raises `RuntimeError`, because it is an
   environmental failure a caller retries rather than a bad argument. R signals
   both through one cli condition class because R has no such distinction.
   Reports are `RuntimeWarning`, the class `find_terms()` already uses.
2. **The vocabulary field list is derived, not restated.** R keeps its own
   `.ms_closure_vocabulary_fields()` and pins it against the verifier by
   evaluating the verifier's body, because R cannot import a constant out of a
   function. Python can, so `_VOCABULARY_FIELDS = tuple(eml._VOCABULARY_SNAPSHOT_FIELDS)`
   and the two cannot diverge at all. The test asserts the derivation, so
   replacing it with a literal fails.

## What I did not do, and why

- **Did not add `write_sdp_semantic_closure` to `_quarto.yml`.** The whole
  publication surface is already absent from the quartodoc reference —
  `write_eml_from_sdp`, `publish_sdp_to_knb`, `write_sdp_sssom`,
  `write_sdp_observation_structures`, `write_sdp_measurement_decompositions`,
  `write_sdp_reproducibility_manifest`. Adding one of seven would be
  inconsistent; adding all seven is scope this item does not carry. The item's
  `retires_when` names `guides/semantic-review.qmd`, which is done. Candidate
  below.
- **Did not bump the version or touch the mirror-contract paragraph.** This is a
  post-0.5.0 behaviour; the S5 window and its bump are **B-153**.
- **Did not update metasalmon's `knowledge/parity-deviations.md`** to mark
  B-165's entry landed, or move the queue item's state. Both are metasalmon-side
  and outside this claim, which covers one branch in `metasalmonpy`.
- **Did not migrate `.hub/workpad.md`** to the per-item path. Candidate below.

## Found, not absorbed

Neither has a queue item that I could find; both are described as candidates
with evidence, per the brief.

1. **Candidate — the test suite only collects from a directory named
   `metasalmonpy`.** `tests/__init__.py` makes `tests` a package and the
   repository root carries the flat-layout `__init__.py`, so pytest ≥ 8 walks up,
   treats the root as a package and imports it under the *directory's* name. When
   that name is `metasalmonpy` it works; under any other name every test errors
   at setup with `ImportError: attempted relative import with no known parent
   package`. Measured on clean `origin/main` `d471dd0`, pytest 8.4.2 and 9.1.1:
   from `…/salmon-data-mobilization-metasalmonpy-B-165` **908 errors, 0 passed**;
   from `…/metasalmonpy` **907 passed, 1 skipped**. pytest 7 is unaffected, and
   `pyproject.toml` pins only `pytest>=7`. CI is green by accident —
   `actions/checkout` happens to check out into a directory named for the repo.
   **This bites the hub directly**: `HUB.md` § *Isolation* keys a worktree
   `<owner>-<repo>-<id>`, which is exactly a name that breaks it. I worked around
   it by nesting the checkout at
   `hub-worktrees/salmon-data-mobilization-metasalmonpy-B-165/metasalmonpy`, and
   the next agent will hit it again. A fix is one line
   (`[tool.pytest.ini_options] consider_namespace_packages`, or removing
   `tests/__init__.py`, or `--import-mode=importlib` with a rootdir setting) but
   it changes how the whole suite is collected, so it wants its own item.
2. **Candidate — `.hub/workpad.md` here was never migrated to the per-item
   path.** metasalmon deleted its shared `.hub/workpad.md` and now carries
   `.hub/workpads/B-116.md`; metasalmonpy still has the shared file, holding
   B-126's report. That is **B-140**'s fix, applied in one repository and not the
   other. I wrote this report to `.hub/workpads/B-165.md` per current `HUB.md`
   and left the old file alone rather than editing another item's record.

## Needing Brett

Nothing. No ontology term IRI was chosen, changed or removed; no public
signature, frozen column contract, version, release, parity-register row or
guard scope was touched; no guard was weakened. The two Python-native choices
above are language idiom under the *parity is behavioural, not literal* rule and
are recorded rather than registered — if either should instead be a deviation
row, that is a decision, and this section is where it would have gone.
