# B-124 — Port the validator checks of #49 to metasalmonpy

Claim `a-22bcf22fcc16e5e6`, session key `fleet-2026-09-16-B-124`, branch
`agent/B-124/a-22bcf22fcc16e5e6`, worktree
`hub-worktrees/salmon-data-mobilization-metasalmonpy-B-124/metasalmonpy`.

## What changed, and where

`package_io.py`

* `_package_metadata_frames()` — new. Mirrors `.ms_package_metadata_frames()`:
  one spelling of the four metadata frames with their issue category, source
  name and identifying fields, so a message and its issue row cannot name
  different files.
* `_collect_blank_required_metadata_fields(package, keys=False)` — new. Mirrors
  `.ms_collect_blank_required_metadata_fields()`. Reads
  `sdp_schema_required_field_names()` (the bundled schema, never the remote one
  — the validator makes no network call) and splits it against
  `METADATA_KEY_FIELDS`. A column the file does not have is blank in every row,
  in all four files. Deferred import of `sdp_field_setters`, because that module
  already reaches back into this one the same way.
* `_validate_optional_sdp_semantic_artifacts(path)` — new. Mirrors
  `.ms_validate_optional_sdp_semantic_artifacts()`: calls `validate_sdp_sssom()`
  and `validate_sdp_measurement_decompositions()` when their managed files are
  present. Presence is by file name, never by scanning `metadata/semantic/`; a
  dangling symlink counts as present.
* `_collect_missing_table_observation_unit_iri_issues()` — an absent
  `observation_unit_iri` column is now blank in every row rather than no rows at
  all.
* `_collect_package_validation_issues()` — two additions. Blank **key** fields
  become typed issues in every mode. Blank **non-key** required fields warn in
  the default mode, on the placeholder channel. Tidy check 4: a column the
  dictionary declares `required` and that ships missing values becomes a
  `columns` issue.
* `validate_salmon_datapackage()` — calls
  `_validate_optional_sdp_semantic_artifacts()` after the observation-structure
  check; appends the blank non-key required messages to the strict-mode issue
  list; the strict-mode advice line now names blank schema-required fields;
  docstring rewritten to say what the function checks, mirroring R's.

`tests/test_validation_hardening.py` — eight new tests plus one new differential
fixture case and one amended one (below). `CHANGELOG.md` — one `Fixed` entry.

## Commands run, and results

Two dependency legs, as `.github/workflows/parity.yml` runs them.

| leg | before | after |
|---|---|---|
| `python -m pytest -q` with `[test]` (core deps; `yaml lxml openpyxl pypdf xlrd` all absent) | 794 passed, 114 skipped | **803 passed, 114 skipped** |
| `python -m pytest -q` with `[test,eml,context]` (all five extras present) | — | **916 passed, 1 skipped** |
| `python tests/smoke.py`, both legs | — | passed |
| `python -m build` | — | built sdist + wheel |
| `python -m pytest -q tests/test_roundtrip.py` with `R_LIBS` pointing at an installed metasalmon 0.5.0 | — | 2 passed |

### Failing before

The new tests were copied onto a pristine `origin/main` worktree
(`d471dd0`) and run there:

```
FAILED tests/test_validation_hardening.py::test_every_issue_category_matches_r_field_for_field[pk-missing-values]
FAILED tests/test_validation_hardening.py::test_every_issue_category_matches_r_field_for_field[required-column-blank]
FAILED tests/test_validation_hardening.py::test_a_required_column_must_not_ship_missing_values
FAILED tests/test_validation_hardening.py::test_a_blank_schema_required_field_warns_by_default_and_fails_strict
FAILED tests/test_validation_hardening.py::test_a_blank_metadata_key_field_is_structural_in_every_mode
FAILED tests/test_validation_hardening.py::test_an_absent_schema_required_column_is_reported_like_a_blank_one
FAILED tests/test_validation_hardening.py::test_a_blank_dataset_id_is_a_structural_issue_not_a_crash
FAILED tests/test_validation_hardening.py::test_validate_salmon_datapackage_refuses_a_corrupt_sssom_artifact
FAILED tests/test_validation_hardening.py::test_validate_salmon_datapackage_refuses_a_corrupt_decomposition_artifact
9 failed, 29 passed
```

One failing test for each of the three classes `retires_when` names, and one for
each Codex-round refinement #111 carried.

### Both implementations, same input

A green suite is not evidence of a port. metasalmon 0.5.0 was installed from the
`main` checkout into a scratch R library (R 4.3.3) and run over **the same
package directories on disk** that the Python fixtures build.

Required-column nullability, blanking `POP_ID` in row 2 of the shipped example:

```
PY  [tables]  Table 'nuseds_fraser_coho' declares primary key 'POP_ID' but column POP_ID contains missing values.
PY  [columns] Table 'nuseds_fraser_coho' column 'POP_ID' is declared required in column_dictionary.csv but 1 row is missing a value.
R   [tables]  Table 'nuseds_fraser_coho' declares primary key 'POP_ID' but column POP_ID contains missing values.
R   [columns] Table 'nuseds_fraser_coho' column 'POP_ID' is declared required in column_dictionary.csv but 1 row is missing a value.
```

Blanking `SPECIES` (declared required, not part of the key) — one issue each,
identical text. Blanking `dataset.csv$contact_email` — both warn, and the only
difference is cli's bullet rendering against Python's flattened string, which is
how the pre-existing placeholder warning already differs:

```
PY  1 schema-required metadata field is blank: dataset.csv$contact_email. Fill it before publication; require_iris=True reports it as an error.
R   1 schema-required metadata field is blank. x dataset.csv$contact_email i Fill it before publication; `require_iris = TRUE` reports it as an error.
```

Corrupt SSSOM manifest hash, and a decomposition CSV whose manifest was deleted
— both implementations refuse the same directory with the same sentence (R
quotes the path, a cli convention):

```
PY  ValueError: SSSOM mapping set .../approved.sssom.tsv does not match its manifest SHA-256 hash.
R   RAISED:     SSSOM mapping set '.../approved.sssom.tsv' does not match its manifest SHA-256 hash.
PY  FileNotFoundError: Missing decomposition manifest at .../measurement-decompositions.json
R   RAISED:            Missing decomposition manifest at '.../measurement-decompositions.json'.
```

Both also agree the clean example package validates in both modes with no
warnings, which is what makes the single-defect injections above readable.

## What I did not do, and why

* **No `PARITY.md` row.** This is R-shipped-first lag, recorded as owed in
  metasalmon's `knowledge/parity-deviations.md`, not a deliberate difference.
  Nothing found here changed that judgement.
* **No version bump.** The lockstep number is a parity claim and 0.5.0's
  documentation half is still open; `CHANGELOG.md`'s `Unreleased` preamble
  already says so.
* **Did not touch metasalmon.** Closing the "queued as B-124" paragraph in
  `knowledge/parity-deviations.md` and the roadmap release index is a
  metasalmon-repository edit; this item's `repo` is `metasalmonpy`, and
  `HUB.md` forbids widening scope. It belongs with B-124's move to `done`.
* **Did not port the `yaml.load(eval.expr = FALSE)` hardening of #111.** It has
  no Python counterpart to port: `sssom.py` parses the embedded metadata block
  with its own `_parse_yaml_subset()`, which evaluates nothing and never calls
  PyYAML, so there is no code path that could execute an `!expr` tag. R needed
  the argument because `yaml::yaml.load()` follows
  `getOption("yaml.eval.expr")`. Recorded here rather than acted on; see
  *Candidates* below.

## Findings that are not this item

* **Filed as hub B-189** (icebox, metasalmon `155378a`): a test pinning that `sssom.py` never evaluates an
  `!expr` tag, the Python twin of R's
  `test-sssom.R::"validate_salmon_datapackage never evaluates an !expr tag"`.
  The property holds today by construction, and #49 is exactly the shape of
  defect where a validator starts reaching collaborator-authored bytes it did
  not reach before — which is why R added the test rather than relying on the
  default. Not filed as scope creep here; worth an item.
* **Observation, not a defect:** `read_salmon_datapackage()` aligns
  `tables.csv` and `dataset.csv` to the full schema column set, where R reads
  those two as written. That is why
  `test_an_absent_observation_unit_iri_column_is_refused_like_a_blank_one`
  passed before this change. The collector no longer depends on it.

## Guards added, and what retires them

Nothing here suppresses, skips or excludes a signal, so nothing needs a
retirement condition on those grounds. Two things are load-bearing enough to
say what would end them:

* The **deferred import** of `sdp_field_setters` inside
  `_collect_blank_required_metadata_fields()` exists only to keep `package_io`
  free of a module-level dependency on a module that imports it back.
  *Retires when:* `METADATA_KEY_FIELDS` / `METADATA_SCHEMA_TABLES` move to a
  module both can import at the top level.
* The **`_SCHEMA_SOURCE = "vendored"` read** is the no-network guarantee, not a
  performance choice. *Retires when:* `load_sdp_schema()` stops fetching on its
  default source — the same condition `sdp_schema.py` already records.

## Codex round on pull request #29 (2026-09-16)

One P2 finding, verified before fixing, and it held. Fixed in `1773e3b`.

**The finding.** `value_type` and `column_role` are `constraints.required`
non-keys of `column_dictionary.csv`. This change warns for them in the default
mode, and then `validate_dictionary()` rejected the retained empty string as an
invalid enum — so `require_iris=False` still raised for two of the very fields
the change documents as warn-by-default.

**Reproduced**, blanking one dictionary row of the shipped example:

```
blank value_type,  require_iris=False -> ValueError: Invalid value_type in rows [3]: ['']
blank column_role, require_iris=False -> ValueError: Invalid column_role in rows [3]: ['']
```

**What decided the remedy: R, on the same directories.** metasalmon 0.5.0
returned with the warning and raised only under strict. R warning while Python
raised makes this the **port being incomplete**, not a design choice, so Python
moved and the contract sentence stands. Had R raised too, the contract sentence
would have been the thing that was wrong and both sides would have had to move,
which is not one agent's call.

**Cause.** `read_sdp_csv()` keeps an empty CSV cell as `""` where R's
`.ms_read_metadata_csv()` reads it as `NA`, and R's enum check exempts `NA`
explicitly. A blank never reaches R's check; it reached this one and was
rejected as an invalid *token*. The enum checks now read a blank as **absent**,
the same reading the `required` check three blocks below already applied.

**After**: warns by default, errors under strict, and `'bogus'` is still refused
in both modes. Pinned by
`test_a_blank_required_enum_warns_by_default_like_every_other_blank`. Both legs
green: 804 core, 917 extras.

## Needs Brett

**The merge of #29 is his, and the PR is back to draft.** Not because the fix is
doubtful — it is measured against R on identical input — but because of what it
does to an existing user: for a blank `value_type` or `column_role`, default-mode
validation goes from **error** to **warning**. `HUB.md`'s *Which pull requests
need Brett* reserves anything that "relaxes, narrows, disables, or deletes a
guard, test, skip condition or validator" to him, and this is that shape, even
though (a) it aligns a Python-only check to its measured R counterpart, (b) the
coverage is recovered by the new collector in the same change, and (c) the strict
answer is unchanged. I could not settle which side of the line it falls on, and
that section says uncertainty resolves toward asking.

*Recommendation:* take it as written. R's behaviour is the ruled one, it was
measured rather than inferred, and nothing that used to be refused under
`require_iris=True` is accepted now.

## Closing state

* **Pull request #29** — draft, `agent/B-124/a-22bcf22fcc16e5e6` at `c0eebdb`.
  All five checks green on that head. Codex's one P2 answered, fixed and the
  thread resolved. **Unmerged on purpose**: see *Needs Brett* above.
* **`origin/main` merged in at `c0eebdb`** after B-125 landed as #30. One
  conflict, in `CHANGELOG.md`, and only because both entries insert at the same
  `### Fixed` anchor; resolved as a union, since they are reports of different
  items and disagree about nothing. Resolved rather than left, because a
  conflicted pull request gets **no** workflow runs at all — an empty check list
  reads as "nothing failed" — and merging the base is both the fix and what
  restarts CI. Confirmed: zero runs while conflicted, all four starting within a
  minute of the resolution. On the merged tree: 810 core / 923 extras, smoke
  passing.
* **Queue state** — B-124 and B-125 set to `review` on metasalmon `main`
  (`ed92a95`). Both claims are still held, by design.
* **Findings filed** — B-188 and B-189, icebox, metasalmon `155378a`. Neither
  promoted: promotion rests on an authorization Brett gives in chat.

---

## Second pass, 2026-09-16 — unblocking the pull request

Brett approved #29 on its merits and asked for it to be marked ready so Codex
reviews it. The merge stays his.

### What was wrong

#29 was **19 commits behind `main`** with `mergeable_state: dirty`. Exactly one
file conflicted, `CHANGELOG.md`; `git merge-tree --write-tree origin/main
<head>` showed everything else merging cleanly. That matters more than it looks:
a conflicted pull request gets **zero** `pull_request` workflow runs — an empty
check list, not a red one — so marking it ready while conflicted would have
started a Codex review against a tree GitHub cannot build a merge ref for.
Resolve first, then mark ready.

### The conflict, and where B-124's entry ended up

`main` gained two things since the last merge at `c0eebdb`: **#33 (B-153)**,
which renamed `## Unreleased` to `## 0.5.0` and set `__version__` to `0.5.0`,
and **#32 (B-144)**, which added a `Fixed` entry at the same `### Fixed` anchor
this branch writes to. So the conflict is not the previous one repeating: the
anchor itself moved into a version heading.

**B-124's entry is under a new `## Unreleased` heading, above `## 0.5.0`, and
this is the load-bearing decision of the pass.** Taking git's "ours" side would
have filed it under `## 0.5.0`, which would be a false claim:

* `AGENTS.md`'s release contract says to **tag the commit that made the version
  current, not a later docs-only merge**. The commit that made `0.5.0` current
  is `19f467b`, and it does not contain this change.
* Measured 2026-09-16: there is no `v0.5.0` tag (`git ls-remote --tags`) and no
  `v0.5.0` GitHub Release — the newest of each is `v0.4.0`. So `## 0.5.0` is a
  *claimed number*, not a shipped release, and an entry added to it now says a
  version contains something the version does not.
* The file's own convention is an accumulator section that is renamed at bump
  time. It had one until `19f467b` consumed it. metasalmon's `NEWS.md` keeps the
  same shape with its *(development version)* heading.

Verified adversarially rather than by reading the markers: with the conflict
resolved to "theirs", the file is **byte-identical to `origin/main`'s
`CHANGELOG.md`** (`diff -q`, clean). So nothing of B-144's, B-125's or B-153's
was moved, reworded or dropped, and the only change to `main`'s content is the
inserted section. Heading order after the resolution: `Unreleased`, `0.5.0`,
`0.4.0`, `0.2.1`, … — the file's existing newest-first order.

**No version number moves on this branch.** The merge takes `main`'s `0.5.0` in
all six enumerated places; this branch changes none of them.

### Verification

Both dependency legs, as `.github/workflows/parity.yml` runs them, and both
against a **clean `origin/main` worktree** (`b939fd9`) so that pre-existing
failures are distinguishable from new ones. Both venvs were reinstalled with
`pip install -e .` after the merge — the editable dist metadata still said
`0.4.0`, which `test_public_version_matches_package_metadata` reads.

| leg | clean `origin/main` (`b939fd9`) | merged head (`cd6e3f7`) |
|---|---|---|
| core dependencies only (`[test]`; `yaml lxml openpyxl pypdf xlrd` verified absent) | 811 passed, 142 skipped | **821 passed, 142 skipped** |
| with `[eml]` and `[context]` (all five verified present) | 952 passed, 1 skipped | **962 passed, 1 skipped** |

**Zero failures on either side of either leg**, so there are no pre-existing
failures to attribute and none introduced. The `+10` is this branch's nine new
tests plus the added differential fixture case.

Also on the merged head: `python tests/smoke.py` passes on **both** legs;
`python -m build` produces `metasalmonpy-0.5.0.tar.gz` and the wheel;
`uv lock --check` clean; `git diff --check` clean; no conflict markers anywhere
in the tree.

`python3 scripts/check-parity-registers.py` (run from metasalmon against this
tree's `PARITY.md`): *parity registers agree: 61 rows, 1-61 with no gaps*. This
branch does not touch `PARITY.md`, which is the claim being checked.

The repository configures no linter or type checker — CI runs pytest, the smoke
workflow, `python -m build`, the docs render and the R parity job, and nothing
else — so there is no lint step to report.

### The one other edit

`.hub/workpads/B-125.md` — the B-188 bullet this branch already rewrote had
unbalanced `**`, so it rendered with stray asterisks. Repaired in place. It is
this branch's own typo, not a second item's content.

### Findings that are not this item

* **`main`'s `## 0.5.0` section has the same problem this pass avoided.**
  B-144's `Fixed` entry (#32) merged *after* `19f467b` made `0.5.0` current, and
  sits under `## 0.5.0`. Under the "tag the commit that made the version
  current" rule it describes a change that commit does not contain. Not touched
  here — it is another item's merged content and moving it would be widening
  scope — but it is the second instance in one day, so the general fix is
  probably a rule in `AGENTS.md`'s *Releases* section saying where an entry goes
  after a bump and before a tag, plus a test. Worth a queue item; not filed from
  here, because filing is a metasalmon-repository edit.

### Guards added, and what retires them

None. This pass adds no suppression, skip, exclusion or workaround.

### The second Codex round (on `b8320af`)

Marking the pull request ready triggered a fresh Codex **code review** (Codex
runs no security review on this repository). One P2 finding, and it is real.

**The finding.** `_present()` in `dictionary.py` — the helper the *first* Codex
round added to make a blank enum read as absent — used a parameterless
`.str.strip()`. `read_sdp_csv()` trims only `READR_TRIM_CHARS` (` \t\r\n`,
what `readr`'s `trim_ws = TRUE` trims), so a `value_type` or `column_role`
holding **only** U+00A0 or U+3000 survives the read as a real token.
`_collect_blank_required_metadata_fields()` uses `READR_TRIM_CHARS` and
correctly saw it as *present*, so it did not report it blank; `_present()`
stripped it to `""` and saw it as *absent*, so the enum check skipped it. It
fell through both.

**Reproduced, and measured on both sides of the same four directories.**

| input | `origin/main` (`b939fd9`) | this branch before the fix | metasalmon 0.5.0 |
|---|---|---|---|
| `value_type` = U+00A0 | `ValueError: Invalid value_type in rows [3]: ['\xa0']` | **ACCEPTED, no warning, both modes** | `RAISED: Invalid value_type in rows 4: .` |
| `value_type` = U+3000 | `ValueError: Invalid value_type in rows [3]: ['\u3000']` | **ACCEPTED, both modes** | `RAISED: Invalid value_type in rows 4: <U+3000>.` |
| `column_role` = U+00A0 | `ValueError: Invalid column_role ...` | **ACCEPTED, both modes** | `RAISED: Invalid column_role in rows 4: .` |
| `column_role` = U+3000 | `ValueError: Invalid column_role ...` | **ACCEPTED, both modes** | `RAISED: Invalid column_role in rows 4: <U+3000>.` |

So this was a regression **this pull request introduced**, not a pre-existing
gap: `main` refused all four, R refuses all four, and the branch accepted all
four silently in every mode. R's check is
`!dict$value_type %in% valid_types & !is.na(dict$value_type)`
(`R/dictionary-helpers.R:1334`), and a U+00A0 cell is neither NA nor valid.

**The fix.** `_present()` strips `READR_TRIM_CHARS` rather than every Unicode
space, so "absent" means the same thing in the two collectors and in R. Nine
lines of comment say why, including its retirement condition: it retires when
`read_sdp_csv()` stops being the single reader, because at that point "absent"
has to be re-derived from whatever replaces it.

**Pinned** by `test_a_unicode_whitespace_enum_is_a_token_not_a_blank`, which
asserts *both* halves — the Unicode-whitespace token is refused in both modes,
**and** a genuinely empty cell is still the warn-by-default blank the first
round established. Demonstrated RED: reverting only the one changed expression
gives `Failed: DID NOT RAISE ValueError`, and the blank-enum test from round one
still passes, which is exactly how this defect got written.

**What else is in the same family**, asked because a fix that removes one
instance is not a fix for the defect. `dictionary.py` has one other bare
`.str.strip()`, in the `required`-flag parse, and it is **pre-existing on
`main`** and untouched here. Measured rather than assumed: with `required` set
to U+00A0, and separately to the unparseable token `maybe`, **both**
implementations return in both modes — R and Python agree, so there is no
divergence to fix and nothing to file. That is the only other instance in this
module.

**Suite after the fix**, both legs, same venvs, same verified dependency shapes:

| leg | before this fix (`cd6e3f7`) | after (`b8320af` + fix) |
|---|---|---|
| core dependencies only | 821 passed, 142 skipped | **822 passed, 142 skipped** |
| with `[eml]` and `[context]` | 962 passed, 1 skipped | **963 passed, 1 skipped** |

`tests/smoke.py` passes on both legs; `python -m build` produces the 0.5.0 sdist
and wheel; `git diff --check` clean.

### Needs Brett

* **The merge itself.** Unchanged from the first pass: this pull request moves a
  default-mode *error* to a default-mode *warning* for a blank `value_type` or
  `column_role`, which is the shape `HUB.md` reserves to him. He has approved it
  on its merits; the merge is still his and was not taken here.
