# Workpad: B-166

## Queue item

**B-166: Re-vendor `sdp.rules.yaml` into metasalmonpy once the reworded SOSA
Procedure rules merge.** Claimed 2026-09-25T11:33:55Z by `a-2b7ee15c7b2ae119`,
from metasalmonpy `main` at `012d04b`, with the hub at `fb1a776`. The evidence
pointer is metasalmon's `knowledge/backlog.md`.

The retirement condition is `retires_when` in metasalmon's
`queue/items/B-166.yaml`, and it is not restated here. What this branch puts
against it is `data/schema/sdp.rules.yaml`, which is now git blob `489d46a0`,
the blob smn-data-pkg `main` and metasalmon `main` both carry.

## What changed, and where

- `data/schema/sdp.rules.yaml`: replaced with smn-data-pkg `origin/main`'s
  `schema/sdp.rules.yaml`, written from the git object with `git cat-file blob`
  rather than copied from a working tree or edited by hand.
- `sdp_schema.py` (module docstring), `tests/test_sdp_schema.py` (module
  docstring) and `pyproject.toml` (a comment beside the package-data globs):
  all three said the bundle is a verbatim copy of the `sdp-0.3.0` tag, and this
  change makes that false for one file. The exception, the reason for it and
  what retires it are written once, in `sdp_schema`'s docstring; the other two
  point there rather than restate it. No statement, assertion or glob changed.
- `CHANGELOG.md`: a `### Changed` entry under `## Unreleased`.
- `AGENTS.md`: untouched. The suite counts did not move (below).
- `PARITY.md`: untouched. The item says this is a port and opens no row.
  Neither `PARITY.md` nor the hub's `knowledge/parity-deviations.md` records
  this re-vendor as owed, so there is no landed record to add in either
  (searched both for `B-166`, `sdp.rules` and `re-vendor`). The owed record is
  a paragraph in the hub's `knowledge/roadmap.md`; see *Not this item*.

## Evidence

### Content address

Blob ids from `git rev-parse <rev>:<path>`, md5 from `git cat-file blob` piped
to `md5sum`, so no working-tree conversion is in the measurement.

| copy | ref | md5 | git blob |
|---|---|---|---|
| metasalmonpy `data/schema/sdp.rules.yaml`, before | `main` `012d04b` | `3c702a373409b23f9c58cb1e1a702c06` | `608467c6c1` |
| smn-data-pkg `schema/sdp.rules.yaml` | `sdp-0.3.0` tag, and `90f74c1` | `3c702a373409b23f9c58cb1e1a702c06` | `608467c6c1` |
| smn-data-pkg `schema/sdp.rules.yaml` | `main` `f86d9b4` | `f94d6c8fecb8de72846c8dfecd2adf9f` | `489d46a0b4` |
| metasalmon `inst/extdata/schema/sdp.rules.yaml` | `main` `fb1a776` | `f94d6c8fecb8de72846c8dfecd2adf9f` | `489d46a0b4` |
| metasalmonpy `data/schema/sdp.rules.yaml`, this branch | working tree, then `git hash-object` | `f94d6c8fecb8de72846c8dfecd2adf9f` | `489d46a0b4` |

8506 bytes. The full blob id is `489d46a0b43c07a5979ba53891e1918e384e3378`.

The file's upstream history was measured, not taken from the brief:
`git log origin/main -- schema/sdp.rules.yaml` in smn-data-pkg lists `432f5d0`
and `95a44a8` (B-106) as the only commits after `90f74c1`;
`git diff 432f5d0 origin/main -- schema/sdp.rules.yaml` is empty; and the Q-51
merge `f86d9b4` touches seven files, none of them this one. The change is to
the `description` of `methods_are_sosa_procedures` and
`row_varying_procedures_use_codes`, now folded (`>-`) scalars, plus five
comment lines. No rule `id` or `severity` moved.

### Who reads the file here

- `sdp_schema._load_vendored_sdp_schema()` reads the vendored copy and
  `sdp_schema._fetch_remote_sdp_schema()` fetches `SDP_RULES_PATH` from the
  pinned `sdp-0.3.0` tag. Both pass the text to `_rules_scalars()`, which keeps
  only lines of the form `key: value` at column 0, and
  `_validate_sdp_schema()` then reads `rules["profile"]` and
  `rules["version"]`. Nothing parses the rules list, and nothing reads a rule
  `id`, `severity` or `description`.
- `tests/test_sdp_schema.py`: the first line must be `version: sdp-0.3.0`;
  `_copy_vendored_bundle()` copies the file for the tampering tests; the scanner
  test asserts `version`, `profile`, and that `id` and `severity` are not
  top-level; and the stale-host test greps every file under `data/`.
- `pyproject.toml`'s `schema/*.yaml` glob ships it in the wheel.

`_rules_scalars()` run through the package on both byte sets, from this tree:

```
old scalars: {"version": "sdp-0.3.0", "profile": "https://salmon-data-mobilization.github.io/smn-data-pkg/profiles/salmon-data-package/v0.3/profile.json"}
new scalars: {"version": "sdp-0.3.0", "profile": "https://salmon-data-mobilization.github.io/smn-data-pkg/profiles/salmon-data-package/v0.3/profile.json"}
identical: True
```

So nothing metasalmonpy accepts, rejects or reports changes, and no reported
text changes either: the five new comment lines start with `#`, which the
scanner's key pattern cannot match, and every other new line is indented.

### The suite, on the old bytes and on the new

Python 3.11.15, pytest 9.1.1, pandas 3.0.6. Core is `.[test]`; extras is
`.[test,eml,context]`. Both venvs' editable installs point at another
worktree, so pytest's `tests/conftest.py` binding is what makes each run test
this one; everything outside pytest was run with a `PYTHONPATH` directory
holding a `metasalmonpy` symlink to this worktree, and
`metasalmonpy.__file__` printed that path. `Rscript` is on `PATH` and
`/tmp/metasalmon-lib` exists, so `tests/test_roundtrip.py` runs locally.

| configuration | old bytes (`012d04b`) | new bytes (this branch) |
|---|---|---|
| core, `python -m pytest -q` | 925 passed, 142 skipped | 925 passed, 142 skipped |
| core, `pytest -q` | 925 passed, 142 skipped | 925 passed, 142 skipped |
| extras, `python -m pytest -q` | 1066 passed, 1 skipped | 1066 passed, 1 skipped |
| extras, `pytest -q` | 1066 passed, 1 skipped | 1066 passed, 1 skipped |

The new-bytes runs are of the final tree, docstrings included. Also on the new
tree: `python tests/smoke.py` passed; `python -m build` built both
distributions and the wheel's `metasalmonpy/data/schema/sdp.rules.yaml` is md5
`f94d6c8f...`; `uv lock --check --offline` passed after the `pyproject.toml`
comment edit.

This is a stale copy, not a defect in code, so there is no failing test
before: the before-and-after evidence is the content-address table above.

## What I did not do, and why

- **No other vendored file was re-vendored**, by the item's scope. The one that
  differs upstream is `dataset.schema.json`, below.
- **The hub roadmap's paragraph about this re-vendor was not touched.** It is in
  metasalmon, a claim covers one repository, and its closing condition is met
  only when this branch is on metasalmonpy `main`.
- **`PARITY.md` row 27 was not amended.** Its "verbatim copy of the upstream
  `sdp-0.3.0` tag" is a dated record of S10 chunk A, still true as history, and
  a register row is Brett's to amend.
- **The ADR path in the file's comments does not resolve in this package.** It
  names `docs/adr/0002-sosa-procedure-reachability.md`, which is in
  smn-data-pkg. Rewriting the comment or vendoring the ADR would break the byte
  identity the item asks for, so neither was done; the changelog entry says
  where the path resolves, as metasalmon's `NEWS.md` does.

## Not this item

**Every other file metasalmonpy vendors, compared by git blob** (`main` of
each repository as above, and smn-data-pkg's `sdp-0.3.0` tag):

| file | metasalmonpy | smn-data-pkg `main` | smn-data-pkg tag | metasalmon |
|---|---|---|---|---|
| `profiles/salmon-data-package/v0.3/profile.json` | `1580fdc16` | `1580fdc16` | `1580fdc16` | `1580fdc16` |
| `schema/eml-mapping.schema.json` | `b1d8ef94d` | absent | absent | `b1d8ef94d` |
| `schema/frictionless/metadata/codes.schema.json` | `d739324dc` | `d739324dc` | `d739324dc` | `d739324dc` |
| `schema/frictionless/metadata/column_dictionary.schema.json` | `ef0479947` | `ef0479947` | `ef0479947` | `ef0479947` |
| `schema/frictionless/metadata/dataset.schema.json` | `9fe231f66` | **`0d0d28556`** | `9fe231f66` | `9fe231f66` |
| `schema/frictionless/metadata/observation_components.schema.json` | `802f46945` | `802f46945` | `802f46945` | `802f46945` |
| `schema/frictionless/metadata/observation_structures.schema.json` | `a49fbc9ca` | `a49fbc9ca` | `a49fbc9ca` | `a49fbc9ca` |
| `schema/frictionless/metadata/tables.schema.json` | `ae5ba8ff1` | `ae5ba8ff1` | `ae5ba8ff1` | `ae5ba8ff1` |

- **`dataset.schema.json` is the one that differs, and two smn-data-pkg commits
  moved it, not one.** `deb5dba` (2026-08-21, merged in pull request 6 as
  `47f0e81`) changed the `spec_version` example from `sdp-0.2.0` to
  `sdp-0.3.0` (blob `9fe231f66` to `7adaea368`). `c48d150` (2026-09-16, Q-51,
  merged in pull request 9 as `f86d9b4`) admits an ISO 8601 UTC instant in
  `temporal_start` and `temporal_end`: the pattern, the descriptions and the
  examples (`7adaea368` to `0d0d28556`). metasalmonpy and metasalmon both carry
  the tag's `9fe231f66`, so both are two commits behind, and the second commit
  widens what validation accepts. The copies are B-198 (metasalmon) and B-199
  (metasalmonpy), which wait on B-236.
- **`eml-mapping.schema.json` is not vendored from smn-data-pkg at all.**
  smn-data-pkg has never carried it (`git log --all -- '*eml-mapping*'` is
  empty); metasalmonpy's copy is metasalmon's blob. So it has no smn-data-pkg
  commit to compare against.
- **With this branch, metasalmonpy's bundle is byte-identical to metasalmon's
  for all nine files.** Of the eight that come from smn-data-pkg, both copies
  depart from its `sdp-0.3.0` tag in this rules file only.
- **The hub roadmap paragraph that records this re-vendor as owed closes when
  this merges.** It is in metasalmon's `knowledge/roadmap.md`, under the
  spec-version spread table, and says it and a clause in that table's
  `metasalmonpy` row are both deleted once this copy is the same blob as
  smn-data-pkg's. That is a hub edit after the merge, not part of this branch.
- **This re-vendor departs from the pinned tag, and B-199 is where that
  ends.** The remote loader pins `sdp-0.3.0`, which serves the older wording,
  so online and offline loads now read different rule text. Nothing here reads
  that text, so it changes no result, and metasalmon has carried the same
  departure since its pull request 120. The exception is recorded in
  `sdp_schema`'s docstring with B-199 as what retires it.

## Guards and exceptions added

No guard, skip, suppression or workaround. The one exception recorded is the
docstring's to the verbatim-copy statement, and it names its retirement: the
whole bundle re-vendored from one tag that carries these rules (B-199).
